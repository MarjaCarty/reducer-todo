const initialState = {
  todoList: [],
  todo: { text: "", completed: false, id: "" },
};

const reducer = (state, action) => {
  switch (action.type) {
    default:
      return { state };
  }
};

export default reducer;
